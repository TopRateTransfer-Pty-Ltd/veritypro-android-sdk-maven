*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
nul

# Build artifacts and temp files
hs_err_pid*.log
replay_pid*.log
.kotlin/

# --- build output / local caches (added 2026-09-13) ---
# Gradle build output in every module, not just the repo root.
# '/build' above is root-anchored and never matched 'verity-device-sdk/build/'.
build/
.gradle/
/app_home.png
/app_launch.png
